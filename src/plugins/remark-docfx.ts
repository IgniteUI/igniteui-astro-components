/**
 * `remark-docfx` was removed — its responsibilities were split into
 * dedicated plugins:
 *
 * - {@link ./remark-env-vars.ts} — `{environment:...}` variable substitution
 * - {@link ./remark-html-transforms.ts} — `<code-view>` / divider HTML transforms
 * - {@link ./remark-md-links.ts} — relative `.md` link rewriting
 *
 * This file is intentionally kept as a placeholder (not re-exported from
 * `package.json`) rather than deleted.
 */
export {};
